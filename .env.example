# 复制本文件为 .env.local（本地用 `vercel dev` 调试），或在 Vercel 项目设置里配置同名环境变量。
# 切勿把真实密钥提交到 Git。

# Gmail 账号（发件人，默认也是收件人）
SMTP_USER=xuhaowei2019@gmail.com

# Gmail「应用专用密码」（16 位，不是邮箱登录密码）。
# 需先开启两步验证，再到 https://myaccount.google.com/apppasswords 生成。
SMTP_PASS=your_16_char_app_password

# 可选：接收留言的邮箱。不填则默认发到 SMTP_USER。
CONTACT_TO=xuhaowei2019@gmail.com
